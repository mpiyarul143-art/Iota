# ── Python caches ─────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Secrets / local config (keep local only) ─────────────────
config.py

# ── Local SQLite database (runtime state, not source) ─────────
iota/data/

# ── Packed / backup artifacts ────────────────────────────────
*.zip
iota_old_backup_*/

# ── Local bug-report scratch files ───────────────────────────
BUG_REPORT*.md
